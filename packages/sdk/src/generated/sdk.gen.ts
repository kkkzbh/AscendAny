// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type ClientMeta, formDataBodySerializer, type Options as Options2, type RequestResult, type ServerSentEventsResult, type TDataShape } from './client';
import { client } from './client.gen';
import type { ArchiveAgentNoteData, ArchiveAgentNoteErrors, ArchiveAgentNoteResponses, AttachLspSessionData, AttachLspSessionErrors, CloseLspSessionData, CloseLspSessionErrors, CloseLspSessionResponses, ConsumeEnrollmentClaimData, ConsumeEnrollmentClaimErrors, ConsumeEnrollmentClaimResponses, CreateAgentNoteData, CreateAgentNoteErrors, CreateAgentNoteResponses, CreateChatThreadData, CreateChatThreadErrors, CreateChatThreadResponses, CreateConfigurationVersionData, CreateConfigurationVersionErrors, CreateConfigurationVersionResponses, CreateLspSessionData, CreateLspSessionErrors, CreateLspSessionResponses, CreateOjProblemVersionData, CreateOjProblemVersionErrors, CreateOjProblemVersionResponses, CreateOjSubmissionData, CreateOjSubmissionErrors, CreateOjSubmissionResponses, CreatePintiaImportData, CreatePintiaImportErrors, CreatePintiaImportResponses, EnqueueAgentRunData, EnqueueAgentRunErrors, EnqueueAgentRunResponses, EnqueueSelfAutoAnalysisData, EnqueueSelfAutoAnalysisErrors, EnqueueSelfAutoAnalysisResponses, GetAgentNoteData, GetAgentNoteErrors, GetAgentNoteResponses, GetAgentRunData, GetAgentRunErrors, GetAgentRunResponses, GetCapabilitiesData, GetCapabilitiesErrors, GetCapabilitiesResponses, GetConfigurationData, GetConfigurationErrors, GetConfigurationResponses, GetCurrentAccountData, GetCurrentAccountErrors, GetCurrentAccountResponses, GetExamAnalysisGenerationData, GetExamAnalysisGenerationErrors, GetExamAnalysisGenerationResponses, GetExamData, GetExamErrors, GetExamResponses, GetImportJobData, GetImportJobErrors, GetImportJobResponses, GetLivenessData, GetLivenessErrors, GetLivenessResponses, GetOjProblemData, GetOjProblemErrors, GetOjProblemResponses, GetOjSubmissionData, GetOjSubmissionErrors, GetOjSubmissionResponses, GetReadinessData, GetReadinessErrors, GetReadinessResponses, GetRecommendationReviewContextData, GetRecommendationReviewContextErrors, GetRecommendationReviewContextResponses, GetRecommendationTrainingRunData, GetRecommendationTrainingRunErrors, GetRecommendationTrainingRunResponses, GetSelfAchievementsData, GetSelfAchievementsErrors, GetSelfAchievementsResponses, GetSelfRecommendationData, GetSelfRecommendationErrors, GetSelfRecommendationResponses, GetSelfStudentAnalyticsData, GetSelfStudentAnalyticsErrors, GetSelfStudentAnalyticsResponses, GetStudentLeaderboardData, GetStudentLeaderboardErrors, GetStudentLeaderboardResponses, GetVersionData, GetVersionErrors, GetVersionResponses, IssueEnrollmentClaimData, IssueEnrollmentClaimErrors, IssueEnrollmentClaimResponses, ListAccountSessionsData, ListAccountSessionsErrors, ListAccountSessionsResponses, ListAgentNotesData, ListAgentNotesErrors, ListAgentNotesResponses, ListAuditEventsData, ListAuditEventsErrors, ListAuditEventsResponses, ListChatMessagesData, ListChatMessagesErrors, ListChatMessagesResponses, ListChatThreadsData, ListChatThreadsErrors, ListChatThreadsResponses, ListConfigurationsData, ListConfigurationsErrors, ListConfigurationsResponses, ListConfigurationVersionsData, ListConfigurationVersionsErrors, ListConfigurationVersionsResponses, ListExamsData, ListExamsErrors, ListExamsResponses, ListImportJobsData, ListImportJobsErrors, ListImportJobsResponses, ListManagedAccountsData, ListManagedAccountsErrors, ListManagedAccountsResponses, ListManagedStudentsData, ListManagedStudentsErrors, ListManagedStudentsResponses, ListOjProblemsData, ListOjProblemsErrors, ListOjProblemsResponses, ListRecommendationTrainingRunEventsData, ListRecommendationTrainingRunEventsErrors, ListRecommendationTrainingRunEventsResponses, LoginAccountData, LoginAccountErrors, LoginAccountResponses, LogoutSessionData, LogoutSessionErrors, LogoutSessionResponses, QueueRecommendationTrainingRunData, QueueRecommendationTrainingRunErrors, QueueRecommendationTrainingRunResponses, RefreshSessionData, RefreshSessionErrors, RefreshSessionResponses, ReplaceAgentNoteData, ReplaceAgentNoteErrors, ReplaceAgentNoteResponses, RestoreAgentNoteData, RestoreAgentNoteErrors, RestoreAgentNoteResponses, RevokeAccountSessionData, RevokeAccountSessionErrors, RevokeAccountSessionResponses, RevokeEnrollmentClaimData, RevokeEnrollmentClaimErrors, RevokeEnrollmentClaimResponses, SetManagedAccountStateData, SetManagedAccountStateErrors, SetManagedAccountStateResponses, StreamAgentRunEventsData, StreamAgentRunEventsErrors, StreamAgentRunEventsResponse, StreamAgentRunEventsResponses, StreamExamAnalysisGenerationEventsData, StreamExamAnalysisGenerationEventsErrors, StreamExamAnalysisGenerationEventsResponse, StreamExamAnalysisGenerationEventsResponses, StreamImportEventsData, StreamImportEventsErrors, StreamImportEventsResponse, StreamImportEventsResponses, StreamOjJudgeEventsData, StreamOjJudgeEventsErrors, StreamOjJudgeEventsResponse, StreamOjJudgeEventsResponses, SubmitAuthenticatedFeedbackData, SubmitAuthenticatedFeedbackErrors, SubmitAuthenticatedFeedbackResponses, TestModelConnectionData, TestModelConnectionErrors, TestModelConnectionResponses, UpdateAccountProfileData, UpdateAccountProfileErrors, UpdateAccountProfileResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Check process liveness.
 */
export const getLiveness = <ThrowOnError extends boolean = false>(options?: Options<GetLivenessData, ThrowOnError>): RequestResult<GetLivenessResponses, GetLivenessErrors, ThrowOnError> => (options?.client ?? client).get<GetLivenessResponses, GetLivenessErrors, ThrowOnError>({ url: '/livez', ...options });

/**
 * Check runtime and database readiness.
 */
export const getReadiness = <ThrowOnError extends boolean = false>(options?: Options<GetReadinessData, ThrowOnError>): RequestResult<GetReadinessResponses, GetReadinessErrors, ThrowOnError> => (options?.client ?? client).get<GetReadinessResponses, GetReadinessErrors, ThrowOnError>({ url: '/readyz', ...options });

/**
 * Read immutable build identity.
 */
export const getVersion = <ThrowOnError extends boolean = false>(options?: Options<GetVersionData, ThrowOnError>): RequestResult<GetVersionResponses, GetVersionErrors, ThrowOnError> => (options?.client ?? client).get<GetVersionResponses, GetVersionErrors, ThrowOnError>({ url: '/version', ...options });

/**
 * Read active upload contracts and hard limits.
 */
export const getCapabilities = <ThrowOnError extends boolean = false>(options?: Options<GetCapabilitiesData, ThrowOnError>): RequestResult<GetCapabilitiesResponses, GetCapabilitiesErrors, ThrowOnError> => (options?.client ?? client).get<GetCapabilitiesResponses, GetCapabilitiesErrors, ThrowOnError>({ url: '/api/v2/capabilities', ...options });

/**
 * Authenticate a local account and create a refresh session.
 *
 * Requires the exact configured web Origin because success sets the host-only refresh cookie. The JSON body has a configured hard read deadline.
 */
export const loginAccount = <ThrowOnError extends boolean = false>(options: Options<LoginAccountData, ThrowOnError>): RequestResult<LoginAccountResponses, LoginAccountErrors, ThrowOnError> => (options.client ?? client).post<LoginAccountResponses, LoginAccountErrors, ThrowOnError>({
    url: '/api/v2/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rotate the one-time refresh credential and issue a new access token.
 *
 * Requires the exact configured web Origin and an empty request body.
 */
export const refreshSession = <ThrowOnError extends boolean = false>(options: Options<RefreshSessionData, ThrowOnError>): RequestResult<RefreshSessionResponses, RefreshSessionErrors, ThrowOnError> => (options.client ?? client).post<RefreshSessionResponses, RefreshSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: '__Host-ascendany_refresh',
            type: 'apiKey'
        }],
    url: '/api/v2/auth/refresh',
    ...options
});

/**
 * Revoke the current session and its complete refresh-token family.
 *
 * Requires the exact configured web Origin and an empty request body.
 */
export const logoutSession = <ThrowOnError extends boolean = false>(options: Options<LogoutSessionData, ThrowOnError>): RequestResult<LogoutSessionResponses, LogoutSessionErrors, ThrowOnError> => (options.client ?? client).post<LogoutSessionResponses, LogoutSessionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, {
            in: 'cookie',
            name: '__Host-ascendany_refresh',
            type: 'apiKey'
        }],
    url: '/api/v2/auth/logout',
    ...options
});

/**
 * Read the account bound to the active access-token session.
 */
export const getCurrentAccount = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentAccountData, ThrowOnError>): RequestResult<GetCurrentAccountResponses, GetCurrentAccountErrors, ThrowOnError> => (options?.client ?? client).get<GetCurrentAccountResponses, GetCurrentAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/auth/me',
    ...options
});

/**
 * Update the display name of the authenticated account.
 *
 * Requires an active access token. The server authenticates the token, then revalidates and updates the exact account and session in one database transaction. The JSON body has the configured authentication read deadline, and query parameters are rejected.
 */
export const updateAccountProfile = <ThrowOnError extends boolean = false>(options: Options<UpdateAccountProfileData, ThrowOnError>): RequestResult<UpdateAccountProfileResponses, UpdateAccountProfileErrors, ThrowOnError> => (options.client ?? client).patch<UpdateAccountProfileResponses, UpdateAccountProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/account/profile',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the authenticated account's bounded session history.
 *
 * Requires an active access token. The current account and exact session are revalidated in the same transaction that reads at most 100 sessions. The current session is first. Request bodies and query parameters are rejected.
 */
export const listAccountSessions = <ThrowOnError extends boolean = false>(options?: Options<ListAccountSessionsData, ThrowOnError>): RequestResult<ListAccountSessionsResponses, ListAccountSessionsErrors, ThrowOnError> => (options?.client ?? client).get<ListAccountSessionsResponses, ListAccountSessionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/account/sessions',
    ...options
});

/**
 * Revoke one session and its complete refresh-token family.
 *
 * Requires an active access token. The target must belong to the authenticated account. The exact account, caller session, and target session are locked and revalidated in one database transaction. Request bodies and query parameters are rejected. Revoking the current session also clears this browser's refresh cookie.
 */
export const revokeAccountSession = <ThrowOnError extends boolean = false>(options: Options<RevokeAccountSessionData, ThrowOnError>): RequestResult<RevokeAccountSessionResponses, RevokeAccountSessionErrors, ThrowOnError> => (options.client ?? client).delete<RevokeAccountSessionResponses, RevokeAccountSessionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/account/sessions/{sessionId}',
    ...options
});

/**
 * Submit authenticated product feedback into the durable delivery queue.
 *
 * Requires an active student or admin access token and enabled writes. clientRequestId is the caller-generated idempotency key for the authenticated account. Replaying the same request returns the original submission with created=false; reusing the key with different content is rejected. Accepted feedback and its queued delivery job are committed atomically before the response is returned. String limits are measured in UTF-8 bytes and every present string must already be trimmed.
 */
export const submitAuthenticatedFeedback = <ThrowOnError extends boolean = false>(options: Options<SubmitAuthenticatedFeedbackData, ThrowOnError>): RequestResult<SubmitAuthenticatedFeedbackResponses, SubmitAuthenticatedFeedbackErrors, ThrowOnError> => (options.client ?? client).post<SubmitAuthenticatedFeedbackResponses, SubmitAuthenticatedFeedbackErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/feedback',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Consume a single-use enrollment credential and create a student session.
 *
 * Requires the exact configured web Origin because success creates a host-only refresh session cookie. Missing, malformed, expired, revoked, and previously consumed credentials all return the same public rejection. The JSON body has the configured authentication read deadline.
 */
export const consumeEnrollmentClaim = <ThrowOnError extends boolean = false>(options: Options<ConsumeEnrollmentClaimData, ThrowOnError>): RequestResult<ConsumeEnrollmentClaimResponses, ConsumeEnrollmentClaimErrors, ThrowOnError> => (options.client ?? client).post<ConsumeEnrollmentClaimResponses, ConsumeEnrollmentClaimErrors, ThrowOnError>({
    url: '/api/v2/auth/enrollment-claims/consume',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Issue a single-use enrollment credential for one imported student identity.
 *
 * Requires an active admin access token. Username, student number, and actor binding are checked as one identity boundary. The plaintext credential is returned exactly once; only its SHA-256 digest is stored. The JSON body has the configured authentication read deadline.
 */
export const issueEnrollmentClaim = <ThrowOnError extends boolean = false>(options: Options<IssueEnrollmentClaimData, ThrowOnError>): RequestResult<IssueEnrollmentClaimResponses, IssueEnrollmentClaimErrors, ThrowOnError> => (options.client ?? client).post<IssueEnrollmentClaimResponses, IssueEnrollmentClaimErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/enrollment-claims',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke one unconsumed enrollment credential.
 *
 * Requires an active admin access token. Missing, expired, consumed, and already revoked grants all return the same public conflict response. The request body and query string must be empty.
 */
export const revokeEnrollmentClaim = <ThrowOnError extends boolean = false>(options: Options<RevokeEnrollmentClaimData, ThrowOnError>): RequestResult<RevokeEnrollmentClaimResponses, RevokeEnrollmentClaimErrors, ThrowOnError> => (options.client ?? client).delete<RevokeEnrollmentClaimResponses, RevokeEnrollmentClaimErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/enrollment-claims/{grantId}',
    ...options
});

/**
 * List all local accounts newest first.
 *
 * Requires an active admin access token. The exact admin account and session are revalidated in the same repeatable-read transaction used for the page. Pagination uses the final account ID from the preceding page.
 */
export const listManagedAccounts = <ThrowOnError extends boolean = false>(options?: Options<ListManagedAccountsData, ThrowOnError>): RequestResult<ListManagedAccountsResponses, ListManagedAccountsErrors, ThrowOnError> => (options?.client ?? client).get<ListManagedAccountsResponses, ListManagedAccountsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/accounts',
    ...options
});

/**
 * Disable or restore one local account.
 *
 * Requires an active admin access token and enabled writes. The caller account/session and target account are locked in one serializable transaction. Every state change increments authRevision, disabling revokes all sessions and refresh families, and an immutable audit event is appended. The active administrator cannot disable itself.
 */
export const setManagedAccountState = <ThrowOnError extends boolean = false>(options: Options<SetManagedAccountStateData, ThrowOnError>): RequestResult<SetManagedAccountStateResponses, SetManagedAccountStateErrors, ThrowOnError> => (options.client ?? client).patch<SetManagedAccountStateResponses, SetManagedAccountStateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/accounts/{accountId}/state',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List every imported student identity and current account binding.
 *
 * Requires an active admin access token. The page joins immutable Pintia actor identity, latest active-snapshot display name, optional local account, and the current analytics-head rating. nextCursor is an opaque canonical base64url token for the final student number.
 */
export const listManagedStudents = <ThrowOnError extends boolean = false>(options?: Options<ListManagedStudentsData, ThrowOnError>): RequestResult<ListManagedStudentsResponses, ListManagedStudentsErrors, ThrowOnError> => (options?.client ?? client).get<ListManagedStudentsResponses, ListManagedStudentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/students',
    ...options
});

/**
 * List immutable security and administration audit events newest first.
 *
 * Requires an active admin access token. Pagination uses the canonical positive decimal ID of the final event in the preceding page. Payloads are bounded JSON objects and never expose stored credentials.
 */
export const listAuditEvents = <ThrowOnError extends boolean = false>(options?: Options<ListAuditEventsData, ThrowOnError>): RequestResult<ListAuditEventsResponses, ListAuditEventsErrors, ThrowOnError> => (options?.client ?? client).get<ListAuditEventsResponses, ListAuditEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/audit-events',
    ...options
});

/**
 * List immutable configuration heads in canonical key order.
 *
 * Requires an active admin access token. Results are ordered by configuration key using bytewise C collation. afterKey is the exact key returned as nextCursor by the preceding page. Unknown, malformed, and duplicated query parameters are rejected.
 */
export const listConfigurations = <ThrowOnError extends boolean = false>(options?: Options<ListConfigurationsData, ThrowOnError>): RequestResult<ListConfigurationsResponses, ListConfigurationsErrors, ThrowOnError> => (options?.client ?? client).get<ListConfigurationsResponses, ListConfigurationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/configurations',
    ...options
});

/**
 * Create and activate one immutable configuration version.
 *
 * Requires an active admin access token and enabled writes. The supplied expectedHeadRevision is a compare-and-swap boundary. The document root must be an object, is canonicalized before hashing, and may contain at most 262144 UTF-8 bytes before canonicalization. model_connection and feedback_delivery credentials are referenced only through credentialRef; credential-bearing document fields are rejected. Recommendation training and knowledge-catalog documents pass their exact server semantic validator before storage. A replay of the active immutable document returns idempotent=true without creating a version.
 */
export const createConfigurationVersion = <ThrowOnError extends boolean = false>(options: Options<CreateConfigurationVersionData, ThrowOnError>): RequestResult<CreateConfigurationVersionResponses, CreateConfigurationVersionErrors, ThrowOnError> => (options.client ?? client).post<CreateConfigurationVersionResponses, CreateConfigurationVersionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/configurations/versions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Test one active immutable model connection.
 *
 * Requires an active admin access token and enabled writes. The request has no body or query parameters. The server loads the active immutable model_connection version, resolves its credential server-side, and performs one bounded provider request through the same HTTPS transport and protocol validator used by online inference. The response contains configuration provenance, provider authority, model name, and latency; credentials and provider response bodies are never returned. This route is absent when writes are disabled.
 */
export const testModelConnection = <ThrowOnError extends boolean = false>(options: Options<TestModelConnectionData, ThrowOnError>): RequestResult<TestModelConnectionResponses, TestModelConnectionErrors, ThrowOnError> => (options.client ?? client).post<TestModelConnectionResponses, TestModelConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/model-connections/{key}/test',
    ...options
});

/**
 * Queue one immutable recommendation training run.
 *
 * Requires an active admin access token and enabled writes. The server revalidates the complete signed principal while atomically snapshotting the current succeeded analytics head, the active immutable training configuration selected by its canonical key, and the exact knowledge catalog version referenced by that configuration. Replays of the same immutable provenance return the existing run. This route is absent when writes are disabled.
 */
export const queueRecommendationTrainingRun = <ThrowOnError extends boolean = false>(options: Options<QueueRecommendationTrainingRunData, ThrowOnError>): RequestResult<QueueRecommendationTrainingRunResponses, QueueRecommendationTrainingRunErrors, ThrowOnError> => (options.client ?? client).post<QueueRecommendationTrainingRunResponses, QueueRecommendationTrainingRunErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/recommendation/training-runs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read the immutable problem identities requiring catalog review.
 *
 * Requires an active admin access token. The response is built in one repeatable-read transaction from the current succeeded analytics head. Problems are deduplicated and strictly sorted by problemKey.
 */
export const getRecommendationReviewContext = <ThrowOnError extends boolean = false>(options?: Options<GetRecommendationReviewContextData, ThrowOnError>): RequestResult<GetRecommendationReviewContextResponses, GetRecommendationReviewContextErrors, ThrowOnError> => (options?.client ?? client).get<GetRecommendationReviewContextResponses, GetRecommendationReviewContextErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/recommendation/review-context',
    ...options
});

/**
 * Read one durable recommendation training run.
 */
export const getRecommendationTrainingRun = <ThrowOnError extends boolean = false>(options: Options<GetRecommendationTrainingRunData, ThrowOnError>): RequestResult<GetRecommendationTrainingRunResponses, GetRecommendationTrainingRunErrors, ThrowOnError> => (options.client ?? client).get<GetRecommendationTrainingRunResponses, GetRecommendationTrainingRunErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/recommendation/training-runs/{runId}',
    ...options
});

/**
 * Read a durable ordered page of training run events.
 */
export const listRecommendationTrainingRunEvents = <ThrowOnError extends boolean = false>(options: Options<ListRecommendationTrainingRunEventsData, ThrowOnError>): RequestResult<ListRecommendationTrainingRunEventsResponses, ListRecommendationTrainingRunEventsErrors, ThrowOnError> => (options.client ?? client).get<ListRecommendationTrainingRunEventsResponses, ListRecommendationTrainingRunEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/recommendation/training-runs/{runId}/events',
    ...options
});

/**
 * Read one configuration and its active immutable version.
 *
 * Requires an active admin access token. Request bodies and query parameters are rejected.
 */
export const getConfiguration = <ThrowOnError extends boolean = false>(options: Options<GetConfigurationData, ThrowOnError>): RequestResult<GetConfigurationResponses, GetConfigurationErrors, ThrowOnError> => (options.client ?? client).get<GetConfigurationResponses, GetConfigurationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/configurations/{key}',
    ...options
});

/**
 * List immutable versions of one configuration newest first.
 *
 * Requires an active admin access token. beforeNumber is the exact nextBeforeNumber returned by the preceding page. Unknown, malformed, and duplicated query parameters are rejected.
 */
export const listConfigurationVersions = <ThrowOnError extends boolean = false>(options: Options<ListConfigurationVersionsData, ThrowOnError>): RequestResult<ListConfigurationVersionsResponses, ListConfigurationVersionsErrors, ThrowOnError> => (options.client ?? client).get<ListConfigurationVersionsResponses, ListConfigurationVersionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/configurations/{key}/versions',
    ...options
});

/**
 * List the authenticated student's current note heads newest first.
 *
 * Requires an active student access token. The server derives the owner from the signed principal and revalidates its account, session, authorization revision, role, and imported student binding in the same repeatable-read snapshot used for the page. cursor is the exact opaque nextCursor returned by the preceding page. Unknown, malformed, and duplicated query parameters are rejected.
 */
export const listAgentNotes = <ThrowOnError extends boolean = false>(options?: Options<ListAgentNotesData, ThrowOnError>): RequestResult<ListAgentNotesResponses, ListAgentNotesErrors, ThrowOnError> => (options?.client ?? client).get<ListAgentNotesResponses, ListAgentNotesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/notes',
    ...options
});

/**
 * Create the first immutable revision of one student-owned note.
 *
 * Requires an active student access token and enabled writes. mutationId is unique within the authenticated account and expectedHeadRevision must be zero. Exact replay returns the existing owned note with idempotent=true; reuse with changed canonical input is rejected. Title and content must already be NFC UTF-8, content must use LF line endings, and limits are measured in UTF-8 bytes.
 */
export const createAgentNote = <ThrowOnError extends boolean = false>(options: Options<CreateAgentNoteData, ThrowOnError>): RequestResult<CreateAgentNoteResponses, CreateAgentNoteErrors, ThrowOnError> => (options.client ?? client).post<CreateAgentNoteResponses, CreateAgentNoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/notes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read one current student-owned note document.
 *
 * Requires an active student access token. The server returns a note only when the revalidated principal owns it. Query parameters and request bodies are rejected.
 */
export const getAgentNote = <ThrowOnError extends boolean = false>(options: Options<GetAgentNoteData, ThrowOnError>): RequestResult<GetAgentNoteResponses, GetAgentNoteErrors, ThrowOnError> => (options.client ?? client).get<GetAgentNoteResponses, GetAgentNoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/notes/{noteId}',
    ...options
});

/**
 * Replace the active document by appending one immutable revision.
 *
 * Requires an active student access token and enabled writes. The owned note must be active and expectedHeadRevision must equal its exact current head. mutationId provides account-scoped exact replay; changed replay is rejected. Title and content use the same canonical text contract as note creation.
 */
export const replaceAgentNote = <ThrowOnError extends boolean = false>(options: Options<ReplaceAgentNoteData, ThrowOnError>): RequestResult<ReplaceAgentNoteResponses, ReplaceAgentNoteErrors, ThrowOnError> => (options.client ?? client).put<ReplaceAgentNoteResponses, ReplaceAgentNoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/notes/{noteId}/document',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Archive an active owned note by appending one immutable revision.
 *
 * Requires an active student access token and enabled writes. The request is a strict compare-and-swap mutation and preserves the current canonical title, content, and content digest.
 */
export const archiveAgentNote = <ThrowOnError extends boolean = false>(options: Options<ArchiveAgentNoteData, ThrowOnError>): RequestResult<ArchiveAgentNoteResponses, ArchiveAgentNoteErrors, ThrowOnError> => (options.client ?? client).post<ArchiveAgentNoteResponses, ArchiveAgentNoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/notes/{noteId}/archive',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore an archived owned note by appending one immutable revision.
 *
 * Requires an active student access token and enabled writes. The request is a strict compare-and-swap mutation and preserves the current canonical title, content, and content digest.
 */
export const restoreAgentNote = <ThrowOnError extends boolean = false>(options: Options<RestoreAgentNoteData, ThrowOnError>): RequestResult<RestoreAgentNoteResponses, RestoreAgentNoteErrors, ThrowOnError> => (options.client ?? client).post<RestoreAgentNoteResponses, RestoreAgentNoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/notes/{noteId}/restore',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enqueue the authenticated student's automatic analysis for one published analytics head.
 *
 * Requires an active student access token and enabled writes. The server owns the dedicated automatic-analysis thread, fixed input content, and every public request/run/message ID. Account-row serialization plus a database uniqueness boundary makes concurrent devices converge on one run per student and analytics generation. An exact repeat with the same configuration keys returns the existing run; different keys for that head are rejected explicitly. The expected revision must equal the current published analytics head in the committing transaction.
 */
export const enqueueSelfAutoAnalysis = <ThrowOnError extends boolean = false>(options: Options<EnqueueSelfAutoAnalysisData, ThrowOnError>): RequestResult<EnqueueSelfAutoAnalysisResponses, EnqueueSelfAutoAnalysisErrors, ThrowOnError> => (options.client ?? client).post<EnqueueSelfAutoAnalysisResponses, EnqueueSelfAutoAnalysisErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/auto-analysis',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the authenticated student's durable chat threads newest first.
 *
 * Requires an active student access token. cursor is the exact thread ID returned as nextCursor by the preceding page. The account, session, authorization revision, role, and imported student binding are revalidated in the same repeatable-read snapshot as the page.
 */
export const listChatThreads = <ThrowOnError extends boolean = false>(options?: Options<ListChatThreadsData, ThrowOnError>): RequestResult<ListChatThreadsResponses, ListChatThreadsErrors, ThrowOnError> => (options?.client ?? client).get<ListChatThreadsResponses, ListChatThreadsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/chat/threads',
    ...options
});

/**
 * Create one empty durable student-owned chat thread.
 *
 * Requires an active student access token and enabled writes. Query parameters and request bodies are rejected.
 */
export const createChatThread = <ThrowOnError extends boolean = false>(options?: Options<CreateChatThreadData, ThrowOnError>): RequestResult<CreateChatThreadResponses, CreateChatThreadErrors, ThrowOnError> => (options?.client ?? client).post<CreateChatThreadResponses, CreateChatThreadErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/chat/threads',
    ...options
});

/**
 * Read an owned chat thread's ordered durable messages.
 *
 * Requires an active student access token. afterSequence is the exact durable message sequence already consumed by the client. Unknown, malformed, and duplicated query parameters are rejected.
 */
export const listChatMessages = <ThrowOnError extends boolean = false>(options: Options<ListChatMessagesData, ThrowOnError>): RequestResult<ListChatMessagesResponses, ListChatMessagesErrors, ThrowOnError> => (options.client ?? client).get<ListChatMessagesResponses, ListChatMessagesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/chat/threads/{threadId}/messages',
    ...options
});

/**
 * Append an input message and enqueue one durable agent run atomically.
 *
 * Requires an active student access token and enabled writes. clientRequestId is idempotent within the authenticated account. The active prompt and model configuration versions, plus the optional analytics generation, are bound immutably when the run is enqueued.
 */
export const enqueueAgentRun = <ThrowOnError extends boolean = false>(options: Options<EnqueueAgentRunData, ThrowOnError>): RequestResult<EnqueueAgentRunResponses, EnqueueAgentRunErrors, ThrowOnError> => (options.client ?? client).post<EnqueueAgentRunResponses, EnqueueAgentRunErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/chat/threads/{threadId}/runs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read one owned durable agent run.
 *
 * Requires an active student access token. Foreign run IDs are not disclosed.
 */
export const getAgentRun = <ThrowOnError extends boolean = false>(options: Options<GetAgentRunData, ThrowOnError>): RequestResult<GetAgentRunResponses, GetAgentRunErrors, ThrowOnError> => (options.client ?? client).get<GetAgentRunResponses, GetAgentRunErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/agent-runs/{runId}',
    ...options
});

/**
 * Resume one owned agent run's ordered durable event stream.
 *
 * Requires an active student access token. Every poll revalidates the principal in the same repeatable-read snapshot as the event head. A cursor above that head is rejected. Full pages are emitted before a terminal stream closes.
 */
export const streamAgentRunEvents = <ThrowOnError extends boolean = false>(options: Options<StreamAgentRunEventsData, ThrowOnError, StreamAgentRunEventsResponse>): Promise<ServerSentEventsResult<StreamAgentRunEventsResponses>> => (options.client ?? client).sse.get<StreamAgentRunEventsResponses, StreamAgentRunEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/agent-runs/{runId}/events',
    ...options
});

/**
 * List visible OJ problem heads in canonical slug order.
 *
 * Requires an active student or admin access token. Students see active problem heads with private solution, specification, and test artifact fields omitted. Admins may explicitly request archived heads. Pagination values are canonical and every query parameter may occur at most once.
 */
export const listOjProblems = <ThrowOnError extends boolean = false>(options?: Options<ListOjProblemsData, ThrowOnError>): RequestResult<ListOjProblemsResponses, ListOjProblemsErrors, ThrowOnError> => (options?.client ?? client).get<ListOjProblemsResponses, ListOjProblemsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/oj/problems',
    ...options
});

/**
 * Read one visible OJ problem head.
 *
 * Requires an active student or admin access token. Student responses omit private solution, specification, and test artifact fields.
 */
export const getOjProblem = <ThrowOnError extends boolean = false>(options: Options<GetOjProblemData, ThrowOnError>): RequestResult<GetOjProblemResponses, GetOjProblemErrors, ThrowOnError> => (options.client ?? client).get<GetOjProblemResponses, GetOjProblemErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/oj/problems/{problemId}',
    ...options
});

/**
 * Create or idempotently replay one immutable OJ problem version.
 *
 * Requires an active admin access token and enabled writes. The closed multipart body contains metadata first and testBundle second. metadata has Content-Type application/json and no filename; testBundle has the exact vendor media type and a safe filename. expectedHeadRevision is a strict compare-and-swap boundary. Repeating the exact canonical content at the same revision returns the existing problem with idempotent=true.
 */
export const createOjProblemVersion = <ThrowOnError extends boolean = false>(options: Options<CreateOjProblemVersionData, ThrowOnError>): RequestResult<CreateOjProblemVersionResponses, CreateOjProblemVersionErrors, ThrowOnError> => (options.client ?? client).post<CreateOjProblemVersionResponses, CreateOjProblemVersionErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/admin/oj/problems/versions',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Queue one idempotent OJ run or submission against an exact problem head.
 *
 * Requires an active student or admin access token and enabled writes. Parts use closed order and media types: metadata, source, then stdin for run mode. submit mode forbids stdin. metadata has no filename; binary parts require safe filenames. clientRequestId is idempotent within the authenticated account and changed reuse is rejected.
 */
export const createOjSubmission = <ThrowOnError extends boolean = false>(options: Options<CreateOjSubmissionData, ThrowOnError>): RequestResult<CreateOjSubmissionResponses, CreateOjSubmissionErrors, ThrowOnError> => (options.client ?? client).post<CreateOjSubmissionResponses, CreateOjSubmissionErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/oj/submissions',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Read one owned OJ submission and its current durable judge state.
 *
 * Requires an active access token. Students may read only their own submissions; admins may inspect every submission.
 */
export const getOjSubmission = <ThrowOnError extends boolean = false>(options: Options<GetOjSubmissionData, ThrowOnError>): RequestResult<GetOjSubmissionResponses, GetOjSubmissionErrors, ThrowOnError> => (options.client ?? client).get<GetOjSubmissionResponses, GetOjSubmissionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/oj/submissions/{submissionId}',
    ...options
});

/**
 * Resume one owned submission's ordered durable judge event stream.
 *
 * Requires an active access token. Authorization is revalidated before response commitment and periodically while connected. Streams have a bounded lifetime; clients resume using the last durable event sequence.
 */
export const streamOjJudgeEvents = <ThrowOnError extends boolean = false>(options: Options<StreamOjJudgeEventsData, ThrowOnError, StreamOjJudgeEventsResponse>): Promise<ServerSentEventsResult<StreamOjJudgeEventsResponses>> => (options.client ?? client).sse.get<StreamOjJudgeEventsResponses, StreamOjJudgeEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/oj/submissions/{submissionId}/events',
    ...options
});

/**
 * Start one isolated bounded clangd session.
 *
 * Available only while the server write capability is enabled; disabled write mode returns 503 writes_disabled before request validation. Requires an active bearer token, one exact configured Origin, an empty body, and no query string. The response returns a high-entropy one-time attach ticket bound to the account, session, and exact Origin. Clients must keep it in memory only and consume it in the immediate WebSocket handshake. The worker has no network, DB client, production credential, or host-execution fallback.
 */
export const createLspSession = <ThrowOnError extends boolean = false>(options?: Options<CreateLspSessionData, ThrowOnError>): RequestResult<CreateLspSessionResponses, CreateLspSessionErrors, ThrowOnError> => (options?.client ?? client).post<CreateLspSessionResponses, CreateLspSessionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/lsp/sessions',
    ...options
});

/**
 * Cancel one owned LSP session and destroy its workspace.
 *
 * Available only while the server write capability is enabled; disabled write mode returns 503 writes_disabled before request validation. Requires an active bearer token, one exact configured Origin, an empty body, and no query string.
 */
export const closeLspSession = <ThrowOnError extends boolean = false>(options: Options<CloseLspSessionData, ThrowOnError>): RequestResult<CloseLspSessionResponses, CloseLspSessionErrors, ThrowOnError> => (options.client ?? client).delete<CloseLspSessionResponses, CloseLspSessionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/lsp/sessions/{sessionId}',
    ...options
});

/**
 * Attach one text-only WebSocket to an owned LSP session.
 *
 * Available only while the server write capability is enabled; disabled write mode returns 503 writes_disabled before request validation. Requires the exact Origin used to create the session, no Authorization header, no query string, and exactly two offered protocols in this order: ascendany.lsp.v1 and ascendany.lsp.ticket.<attachTicket>. The server atomically consumes the ticket before upgrade and selects only ascendany.lsp.v1 in the response. Query or Authorization credentials are rejected. Each text message is one bounded JSON-RPC body; binary messages, duplicate JSON keys, non-workspace file URIs, excess messages, and sessions beyond the hard duration are rejected. Disconnect terminates clangd and deletes the workspace.
 */
export const attachLspSession = <ThrowOnError extends boolean = false>(options: Options<AttachLspSessionData, ThrowOnError>): RequestResult<unknown, AttachLspSessionErrors, ThrowOnError> => (options.client ?? client).get<unknown, AttachLspSessionErrors, ThrowOnError>({ url: '/api/v2/lsp/sessions/{sessionId}/websocket', ...options });

/**
 * Read the current student's canonical analytics profile and history.
 *
 * Requires an active student access token. The caller cannot supply an account, actor, or student identifier. The server derives account ID, role, and authorization revision from the active session and revalidates that complete principal against the student actor binding in the same read-only database snapshot used for analytics. Histories contain the most recent requested observations in chronological order.
 */
export const getSelfStudentAnalytics = <ThrowOnError extends boolean = false>(options?: Options<GetSelfStudentAnalyticsData, ThrowOnError>): RequestResult<GetSelfStudentAnalyticsResponses, GetSelfStudentAnalyticsErrors, ThrowOnError> => (options?.client ?? client).get<GetSelfStudentAnalyticsResponses, GetSelfStudentAnalyticsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/analytics',
    ...options
});

/**
 * Read the authenticated student's derived achievement progress.
 *
 * Requires an active student access token and an empty body and query. The server revalidates the complete principal and student binding in the same repeatable-read snapshot used to read the active immutable rule set, current analytics head, and successful reply count. Progress is derived on read; no per-student achievement state is persisted. All active rules are returned for not_generated, no_observations, and ready states.
 */
export const getSelfAchievements = <ThrowOnError extends boolean = false>(options?: Options<GetSelfAchievementsData, ThrowOnError>): RequestResult<GetSelfAchievementsResponses, GetSelfAchievementsErrors, ThrowOnError> => (options?.client ?? client).get<GetSelfAchievementsResponses, GetSelfAchievementsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/achievements',
    ...options
});

/**
 * Read the authenticated student's current recommendation.
 *
 * Requires an active student access token and an empty body and query. The server revalidates the complete signed principal and student actor binding in the same repeatable-read snapshot used to read both heads, model provenance, and the typed result-v2 learning path. unavailable is a successful product state. fresh means both analytics generation and head revision exactly match the active model source; all other valid active-model reads are stale.
 */
export const getSelfRecommendation = <ThrowOnError extends boolean = false>(options?: Options<GetSelfRecommendationData, ThrowOnError>): RequestResult<GetSelfRecommendationResponses, GetSelfRecommendationErrors, ThrowOnError> => (options?.client ?? client).get<GetSelfRecommendationResponses, GetSelfRecommendationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/me/recommendation',
    ...options
});

/**
 * Read the canonical student leaderboard from the published analytics head.
 *
 * Requires an active student access token. The server verifies the signed access credential, then revalidates its exact account, session, authorization revision, and role in the same read-only database snapshot used to read the published analytics head and leaderboard rows. Results use competition ranking and are ordered by descending rating, then ascending student number.
 */
export const getStudentLeaderboard = <ThrowOnError extends boolean = false>(options?: Options<GetStudentLeaderboardData, ThrowOnError>): RequestResult<GetStudentLeaderboardResponses, GetStudentLeaderboardErrors, ThrowOnError> => (options?.client ?? client).get<GetStudentLeaderboardResponses, GetStudentLeaderboardErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/students/leaderboard',
    ...options
});

/**
 * List active immutable exam snapshots newest first.
 *
 * Requires an active student or admin access token. The server verifies the signed principal, then revalidates the exact account, session, authorization revision, role, and student binding in the same repeatable-read transaction used for the catalog page. Pagination uses the final exam ID returned by the preceding page.
 */
export const listExams = <ThrowOnError extends boolean = false>(options?: Options<ListExamsData, ThrowOnError>): RequestResult<ListExamsResponses, ListExamsErrors, ThrowOnError> => (options?.client ?? client).get<ListExamsResponses, ListExamsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/exams',
    ...options
});

/**
 * Read one active immutable exam snapshot and problem aggregates.
 *
 * Requires an active student or admin access token. Exam metadata and all problem aggregates are read from the same active snapshot and the same repeatable-read transaction that revalidates the caller principal. Query parameters and request bodies are rejected.
 */
export const getExam = <ThrowOnError extends boolean = false>(options: Options<GetExamData, ThrowOnError>): RequestResult<GetExamResponses, GetExamErrors, ThrowOnError> => (options.client ?? client).get<GetExamResponses, GetExamErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/exams/{examId}',
    ...options
});

/**
 * Read the current analytics generation bound to an exam head.
 *
 * Requires an active student or admin access token and an empty body and query. The server revalidates the principal and selects only the newest generation whose target snapshot and target exam head revision still match the exam's active immutable head.
 */
export const getExamAnalysisGeneration = <ThrowOnError extends boolean = false>(options: Options<GetExamAnalysisGenerationData, ThrowOnError>): RequestResult<GetExamAnalysisGenerationResponses, GetExamAnalysisGenerationErrors, ThrowOnError> => (options.client ?? client).get<GetExamAnalysisGenerationResponses, GetExamAnalysisGenerationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/exams/{examId}/analysis-generation',
    ...options
});

/**
 * Resume one explicitly pinned exam generation's ordered durable event stream.
 *
 * Requires an active student or admin access token. Every poll revalidates the principal and verifies that the exact generation belongs to the exam in the same repeatable-read snapshot as the durable event head. The generation selection never falls back to the exam's current generation, so an active-head change cannot reinterpret Last-Event-ID. Full pages are emitted before a terminal stream closes. Clients resume using the last durable event sequence; a cursor above the pinned generation head is rejected.
 */
export const streamExamAnalysisGenerationEvents = <ThrowOnError extends boolean = false>(options: Options<StreamExamAnalysisGenerationEventsData, ThrowOnError, StreamExamAnalysisGenerationEventsResponse>): Promise<ServerSentEventsResult<StreamExamAnalysisGenerationEventsResponses>> => (options.client ?? client).sse.get<StreamExamAnalysisGenerationEventsResponses, StreamExamAnalysisGenerationEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/exams/{examId}/analysis-generations/{generationId}/events',
    ...options
});

/**
 * List durable Pintia import jobs newest first.
 *
 * Requires an active admin access token. Pagination uses the public ID of the last item returned by the preceding page. Unknown, malformed, and duplicated query parameters are rejected. A cursor that no longer identifies a durable job is rejected.
 */
export const listImportJobs = <ThrowOnError extends boolean = false>(options?: Options<ListImportJobsData, ThrowOnError>): RequestResult<ListImportJobsResponses, ListImportJobsErrors, ThrowOnError> => (options?.client ?? client).get<ListImportJobsResponses, ListImportJobsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/imports',
    ...options
});

/**
 * Stream one complete Pintia snapshot v2 into the durable import queue.
 *
 * Requires an active admin access token. Snapshot bytes are durable before the job is returned. The upload body has a configured hard read deadline.
 */
export const createPintiaImport = <ThrowOnError extends boolean = false>(options: Options<CreatePintiaImportData, ThrowOnError>): RequestResult<CreatePintiaImportResponses, CreatePintiaImportErrors, ThrowOnError> => (options.client ?? client).post<CreatePintiaImportResponses, CreatePintiaImportErrors, ThrowOnError>({
    bodySerializer: null,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/imports/pintia',
    ...options,
    headers: {
        'Content-Type': 'application/vnd.ascendany.pintia.snapshot.v2+json',
        ...options.headers
    }
});

/**
 * Read one durable Pintia import job.
 *
 * Requires an active admin access token.
 */
export const getImportJob = <ThrowOnError extends boolean = false>(options: Options<GetImportJobData, ThrowOnError>): RequestResult<GetImportJobResponses, GetImportJobErrors, ThrowOnError> => (options.client ?? client).get<GetImportJobResponses, GetImportJobErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/imports/{jobId}',
    ...options
});

/**
 * Resume the ordered durable event stream after the supplied sequence.
 *
 * Requires an active admin access token. Durable v2 events are retained permanently. Authorization is checked again before the response is committed and periodically while connected. Every stream has a configured maximum lifetime and bounded write duration; clients resume a closed stream with Last-Event-ID.
 */
export const streamImportEvents = <ThrowOnError extends boolean = false>(options: Options<StreamImportEventsData, ThrowOnError, StreamImportEventsResponse>): Promise<ServerSentEventsResult<StreamImportEventsResponses>> => (options.client ?? client).sse.get<StreamImportEventsResponses, StreamImportEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v2/imports/{jobId}/events',
    ...options
});
