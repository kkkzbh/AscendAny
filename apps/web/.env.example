# AscendWeb (Vite) 环境变量示例
# 复制此文件为 .env 并按需修改

# 后端 Origin（可选）。
# - 为空：使用相对路径；本地开发通常走 Vite proxy（见 vite.config.ts）。
# - 非空：用于独立部署/跨域访问（需要后端正确配置 CORS_ALLOWED_ORIGINS）。
VITE_BACKEND_ORIGIN=

# SPA Router basename（默认 /）。
# 例如把站点部署到 /app/ 下时可设为 /app
VITE_APP_BASENAME=/
