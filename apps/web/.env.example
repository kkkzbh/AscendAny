# AscendWeb (Vite) 环境变量示例
# 复制此文件为 .env 并按需修改

# 后端 Origin（可选）。
# - 为空：使用相对路径，仅适合临时本地 API 联调。
# - 非空：用于独立部署/跨域访问，默认直连 km6 API。
VITE_BACKEND_ORIGIN=https://ascendany.kkkzbh.cn

# SPA Router basename（默认 /）。
# 例如把站点部署到 /app/ 下时可设为 /app
VITE_APP_BASENAME=/
